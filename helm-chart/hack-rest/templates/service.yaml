apiVersion: v1
kind: Service
metadata:
  name: {{ include "hack-rest.fullname" . }}
  labels:
    {{- include "hack-rest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hack-rest.selectorLabels" . | nindent 4 }}
