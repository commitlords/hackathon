apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hack-rest.fullname" . }}-test-connection"
  labels:
    {{- include "hack-rest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hack-rest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
